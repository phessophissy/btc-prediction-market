# Frontend Workflow

Recommended workflow for UI changes:

1. Start the app with `npm run dev` in `frontend/`.
2. Make small component-level edits.
3. Re-run `npm run build` before pushing.
4. Keep contract configuration changes isolated from visual changes when possible.
