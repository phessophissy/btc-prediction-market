# Frontend Pages

Current route surfaces:

- `/`: landing page and active markets
- `/markets`: directory-style market listing
- `/create`: market creation flow
- `/portfolio`: connected wallet positions
- `/leaderboard`: ranked wallet view

Each route lives under `frontend/src/app/`.
